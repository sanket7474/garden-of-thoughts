---
const { id, title = "YouTube video", inlineOnMobile = false } = Astro.props;
---
<lite-youtube
  videoid={id}
  title={title}
  playlabel={`Play ${title}`}
  params="rel=0"
  mobile-inline={inlineOnMobile ? 'true' : 'false'}
></lite-youtube>
